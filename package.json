{
  "name": "van-kiep-tu-tien",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vertical slice game tu tien 3D online goc nhin thu ba.",
  "scripts": {
    "dev": "node server/server.js",
    "build": "vite build",
    "start": "node server/server.js --production",
    "test": "node --test tests/*.test.js",
    "check": "npm run test && npm run build",
    "smoke:network": "node scripts/network-smoke.mjs"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  },
  "engines": {
    "node": ">=20"
  }
}
