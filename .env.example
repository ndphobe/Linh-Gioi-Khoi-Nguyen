# Optional override. Local development uses the same localhost origin, while
# production defaults to the repository's Render server. Set this only when
# moving the Node/Socket.IO backend to another HTTPS URL.
VITE_SERVER_URL=https://your-game-server.onrender.com
