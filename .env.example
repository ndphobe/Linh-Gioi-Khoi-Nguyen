# Leave empty for local development (`npm run dev`) because Express and Vite
# share http://localhost:3000. On Vercel, set this to the HTTPS URL of the
# deployed Node/Socket.IO backend, without a trailing slash.
VITE_SERVER_URL=https://your-game-server.onrender.com
